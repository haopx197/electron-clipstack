import styled from "styled-components";

export function IconsTab(): React.JSX.Element {
    return <Wrapper>Icons tab — coming soon.</Wrapper>;
}

const Wrapper = styled.div`
    padding: 32px 16px;
    text-align: center;
    color: var(--color-text);
    opacity: 0.7;
    font-size: 12px;
`;
